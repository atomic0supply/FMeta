import { UserManagementView } from "@/components/UserManagementView";

export const metadata = {
  title: "Equipo — Roqueta",
};

export default function EquipoPage() {
  return <UserManagementView />;
}
